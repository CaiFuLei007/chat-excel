.trae/
.vscode/
build/
.clangd

.cache/
compile_commands.json

# 构建产物输出目录(可执行文件与静态库), 可由 cmake 构建重新生成
bin/

.idea/

*.swp

.DS_Store

# odb 编译器生成产物(数据库支持代码与建表 SQL), 可由 cmake 构建重新生成
data/odb/
data/sql/

# protoc 编译器生成产物(RPC 接口代码), 可由 cmake 构建重新生成
proto/proto_code/

# 运行期上传的 SQLite 测试数据库文件
sqlitefile/